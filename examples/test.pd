

// This is a single line comment

/*
    This is a multi line comment
*/

// Simple program to display the language 

string continue = "yes";

while (continue == "yes")
{
    string name;
    string line = " -------------------------- ";
    string checkHuman;
    int age = 0;
    double weight = 0; 
    bool human = true;

    input ("Enter your full name : ", name);
    input ("Enter your age : ", age);
    input ("Enter your weight :", weight);
    input ("Are you an human (yes/no): ", checkHuman);

    print (line);
    print ("Name : " + name);

    // for the age
    if (age < 0 )
    {
    print ("Age group : Negative age is invalid");
    }
    else if (age <= 18)
    {
        print ("Age group : Child");
    }
    else 
    {
        print ("Age group : Adult");
    }

    // check human
    if (checkHuman == "yes")
    {print ("Species : Human");} 
    else {print ("Species : not human");}

    print (line);

    for (int i = 0, i <= 5, i++)
    {
        printnl (i);        // printnl -- prints with no new line , prints in the same line
    }
    printnl ("\n");
    input ("do you wanna do it again (yes/no)? :", continue); 
}